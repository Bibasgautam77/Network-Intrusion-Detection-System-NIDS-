scapy>=2.5.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
joblib>=1.3.0
pyyaml>=6.0
pydantic>=2.6.0

# Optional, for PostgreSQL instead of the default SQLite:
# psycopg2-binary>=2.9.9

# Optional, for Elasticsearch-backed storage/analytics (not wired in by default):
# elasticsearch>=8.13.0
